"use client";

import { LoginForm } from "@/components";

const Login = () => {
  return (
    <main className="h-full flex justify-center bg-gradient">
      <LoginForm />
    </main>
  );
};

export default Login;
