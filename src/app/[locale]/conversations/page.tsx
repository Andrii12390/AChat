import { ChatPrompt } from "@/components";

const Chat = () => {

  return (
    <main className="h-full pl-72 sm:hidden lg:block md:block">
      <ChatPrompt />
    </main>
  );
};

export default Chat;
