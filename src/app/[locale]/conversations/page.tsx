import { ChatPrompt } from "@/components";

const Chat = () => {
  return (
    <main className="pl-72 h-full sm:hidden lg:block md:block">
      <ChatPrompt />
    </main>
  );
}

export default Chat;